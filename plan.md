# 壁纸快切小组件 — 项目执行计划书

- **项目名称**：壁纸快切小组件（wallswitch）
- **目标产物**：wallswitch-debug.apk（自用安装包）
- **开发语言**：Java 17（最新稳定版）
- **编译方式**：GitHub Actions 云端编译（优先）→ 本地 Android Studio（备选）
- **文档版本**：v1.0
- **日期**：2026-09-22

---

## 一、需求确认清单

| # | 需求 | 说明 |
| :- | :--- | :--- |
| 1 | 切换方式 | 顺序切换 / 随机切换（一轮不重复，用完重置）/ 自动定时切换（30分钟、1小时、6小时、每天、自定义分钟级间隔），用户在设置中选择 |
| 2 | 壁纸范围 | 统一壁纸库，每张图可勾选「应用到桌面 / 应用到锁屏」；桌面、锁屏各自独立推进切换进度，也可单独关闭更换 |
| 3 | 图片来源 | 系统相册多选（单次最多50张），无需存储权限 |
| 4 | 切换反馈 | 点击桌面小组件**直接生效**，无确认弹窗 |
| 5 | 图片编辑 | 添加到壁纸库时捏合缩放 + 拖动定位，含边界钳制（防止露黑边） |
| 6 | 持久化 | 编辑结果保存到App私有目录，重启/清后台后依然有效（暂不做重新编辑） |

### 补充约定（行为定义）

| 事项 | 约定 |
| :--- | :--- |
| 手动与定时关系 | 共用同一切换进度：如库中有图1、2、3，当前显示1，手动点击切到2，到点自动切到3。手动点击同样推进顺序索引/消耗随机池，不重置随机池 |
| 库为空 / 图片不足 | 循环使用现有图片 |
| 库容量上限 | 暂不考虑 |
| 「每天」触发时刻 | 设置页提供时间选择器，默认 08:00 |

---

## 二、技术选型（版本锁定）

| 组件 | 版本/方案 | 说明 |
| :--- | :--- | :--- |
| 开发语言 | Java 17 | 最新稳定语言级别 |
| Gradle | 8.7 | 云端编译使用 |
| Android Gradle Plugin | 8.5.2 | 当前稳定版 |
| minSdk / targetSdk | 26 / 35 | 兼容 Android 8.0 ~ 15 |
| 壁纸设置 | WallpaperManager | 系统API，支持分别设置桌面/锁屏 |
| 桌面小组件 | AppWidgetProvider | 点击即切换 |
| 图片选择 | Photo Picker（PickVisualMedia） | 系统相册选择器 |
| 编辑器 | 自定义 View + Matrix 手势 | 捏合缩放、拖动、边界钳制 |
| 定时任务 | AlarmManager | 精确定时，开机自启恢复 |
| 数据存储 | App私有目录 + SharedPreferences | 卸载才清除 |

---

## 三、项目结构

```text
wallswitch/
├── .github/workflows/build.yml      云端编译配置
├── settings.gradle                  工程配置
├── build.gradle                     根构建脚本
├── gradle.properties                编译参数
├── plan.md                          本执行计划书
└── app/
    ├── build.gradle                 App模块构建脚本
    └── src/main/
        ├── AndroidManifest.xml      组件注册 + 权限声明
        ├── java/com/example/wallswitch/
        │   ├── MainActivity.java    主页：壁纸库 + 设置
        │   ├── EditActivity.java    编辑器页面
        │   ├── CropView.java        缩放/拖动手势View
        │   ├── Switcher.java        切换逻辑核心
        │   ├── WallpaperStore.java  图片持久化存储
        │   ├── AlarmScheduler.java  定时任务管理
        │   ├── AlarmReceiver.java   定时触发接收器
        │   ├── BootReceiver.java    开机恢复定时
        │   └── WidgetProvider.java  桌面小组件
        └── res/
            ├── layout/              布局文件（主页/编辑页/列表项/小组件）
            ├── xml/widget_info.xml  小组件配置
            └── values/strings.xml   字符串资源
```

---

## 四、总体流程

```text
[阶段0] 准备账号与环境
    ↓
[阶段1] 创建仓库 + 上传项目骨架 → 云端编译验证（空壳APK跑通）
    ↓
[阶段2] 核心功能开发（图片库 + 编辑器 + 持久化）
    ↓
[阶段3] 切换逻辑开发（顺序/随机 + 桌面/锁屏独立配置）
    ↓
[阶段4] 桌面小组件开发
    ↓
[阶段5] 定时切换 + 开机自启
    ↓
[阶段6] 整体测试验收 → 下载APK → 安装到手机
    ↓
[阶段7] 后续迭代（改需求 → 上传 → 自动出新APK）
```

---

## 五、分阶段执行清单

### 阶段0：准备工作（约10分钟）

| 任务 | 操作 | 完成标准 |
| :--- | :--- | :--- |
| 0.1 | 注册GitHub账号（免费） | 能登录 github.com |
| 0.2 | 新建仓库 wallswitch，选择 Public | 仓库页面可访问 |
| 0.3 | 手机设置允许安装「未知来源应用」 | 手机可安装非商店APK |

### 阶段1：项目骨架上传与编译验证（约20分钟）

| 任务 | 文件 | 说明 |
| :--- | :--- | :--- |
| 1.1 | .github/workflows/build.yml | 云端自动编译配置 |
| 1.2 | settings.gradle / build.gradle / gradle.properties | 工程构建配置 |
| 1.3 | app/build.gradle / AndroidManifest.xml | App模块配置与权限 |
| 1.4 | 资源文件（布局、strings、widget_info） | UI骨架 |
| 1.5 | 各Java类空壳（保证编译通过的最小实现） | 项目结构完整 |
| **里程碑** | **上传后 Actions 自动编译，产出空壳APK** | 流程打通 |

### 阶段2：图片库与编辑器（核心）

| 任务 | 内容 | 完成标准 |
| :--- | :--- | :--- |
| 2.1 | 接入系统照片选择器，支持多选 | 能一次选多张图进入App |
| 2.2 | 图片复制到App私有目录持久化 | 重启后图片不丢失 |
| 2.3 | 编辑器：捏合缩放 + 拖动位置 + 边界钳制 | 手势流畅，不露黑边 |
| 2.4 | 编辑结果导出保存（私有目录 + 缩略图） | 列表可见缩略图 |
| 2.5 | 壁纸库列表页（删除、应用范围勾选：桌面/锁屏） | 管理功能可用 |
| **里程碑** | **手动把编辑好的图设为壁纸成功** | |

### 阶段3：切换逻辑

| 任务 | 内容 | 完成标准 |
| :--- | :--- | :--- |
| 3.1 | 顺序切换逻辑（索引循环） | 依次切换不跳图 |
| 3.2 | 随机切换逻辑（一轮不重复） | 用完一轮才重置 |
| 3.3 | 桌面/锁屏独立设置生效 | 各自可单独更换或不更换 |
| 3.4 | 切换入口接入（App内按钮预演） | 点击直接生效 |
| **里程碑** | **App内点击即可按设定模式切换壁纸** | |

### 阶段4：桌面小组件

| 任务 | 内容 | 完成标准 |
| :--- | :--- | :--- |
| 4.1 | 小组件布局（1x1，显示当前壁纸缩略图） | 可添加到桌面 |
| 4.2 | 点击小组件 → 触发 switchNext | 壁纸立即切换 |
| 4.3 | 小组件缩略图随切换刷新 | 预览与实际一致 |
| **里程碑** | **桌面点击小组件 → 壁纸立即切换** | |

### 阶段5：定时切换与自启

| 任务 | 内容 | 完成标准 |
| :--- | :--- | :--- |
| 5.1 | 设置页定时频率选项（30分钟/1小时/6小时/每天/自定义分钟数） | 可选择并保存 |
| 5.2 | AlarmManager 定时触发切换 | 到点自动换壁纸 |
| 5.3 | BootReceiver 开机恢复定时 | 重启手机后定时依然生效 |
| 5.4 | 电池优化提示（引导用户允许后台运行） | 荣耀手机不被杀后台 |
| **里程碑** | **定时切换全流程可用** | |

### 阶段6：测试验收与安装

| 任务 | 内容 |
| :--- | :--- |
| 6.1 | 按下方「验收测试清单」逐项自测 |
| 6.2 | 从 Actions 页面下载 APK → 传到手机 → 安装 |
| 6.3 | 添加小组件到桌面，实测一周稳定性 |
| **里程碑** | **全部验收通过，项目交付** |

### 阶段7：后续迭代

改需求 → 我输出修改后的文件 → 上传GitHub → Actions自动出新APK → 下载安装覆盖。

---

## 六、GitHub 云端编译操作流程

```text
第一步：登录 GitHub → 右上角 + → New repository → 命名 wallswitch → Public → 创建
   ↓
第二步：网页端「Add file → Upload files」把所有项目文件拖拽上传（保持目录结构）→ Commit
   ↓
第三步：进入仓库 Actions 标签页，等待自动编译（约3~5分钟，绿灯=成功）
   ↓
第四步：点进该次运行 → 页面底部 Artifacts → 下载 wallswitch-apk
   ↓
第五步：解压得到 app-debug.apk → 传到手机（微信文件传输/QQ/数据线均可）→ 安装
   ↓
第六步：长按桌面空白处 → 添加小组件 → 找到「壁纸快切」→ 拖到桌面
```

**注意**：
- `.github` 是以点开头的文件夹，网页上传时在文件名中输入 `.github/workflows/build.yml` 即可自动创建目录
- 编译失败先看 Actions 页面的红色报错日志，把报错发我即可定位

---

## 七、验收测试清单

| 测试项 | 预期结果 |
| :--- | :--- |
| 从相册一次选择多张图片 | 全部进入壁纸库 |
| 对某张图捏合缩放、拖动后确认 | 效果正确，重启App后不丢失 |
| 点击桌面小组件 | 壁纸立即切换（顺序/随机按设置） |
| 设为随机模式连点多次 | 一轮内不出现重复壁纸 |
| 桌面/锁屏分别配置 | 各自独立变化互不影响 |
| 开启定时切换（如1小时） | 到点自动换，重启手机后依然生效 |
| 删除某张壁纸 | 列表和磁盘同步清除 |
| 添加超大图片 | 不闪退，自动压缩采样 |

---

## 八、风险与预案

| 风险 | 预案 |
| :--- | :--- |
| 云端编译环境版本不匹配 | workflow 已锁定 JDK17 + Gradle8.7 + AGP8.5.2，如冲突按报错降级适配 |
| 荣耀系统对锁屏壁纸API限制 | 使用原生 WallpaperManager 接口；个别机型受限则降级为仅桌面 |
| 定时切换被系统省电杀掉 | AlarmManager + 开机自启 + 引导用户在设置中允许自启动/后台运行 |
| Android 12+ 精确闹钟权限默认拒绝 | 申请 SCHEDULE_EXACT_ALARM 并引导授权；被拒绝则降级为非精确闹钟（setAndAllowWhileIdle，允许少量延迟） |
| GitHub 访问不稳 | 重跑 workflow；或切换本地 Android Studio 编译方案 |
| 相册URI权限失效 | 选图后立即复制到App私有目录，不依赖原始URI |

---

## 九、下一步行动

1. 保存本文件为 `plan.md`
2. 回复「开始阶段一」→ 我输出阶段1的全部项目文件（配置+骨架代码+build.yml）
3. 上传GitHub → 编译验证 → 逐阶段推进
