# wallswitch

定时换壁纸（桌面/锁屏）· 多壁纸库 · 小组件 · 开机自恢复定时

## 上屏方式（v3.10 起）

- **桌面：只走动态壁纸引擎**。把本 App 设为动态壁纸后，桌面由内置 `WallSwitchService` 引擎直接
  解码渲染（借鉴 Muzei 的直接渲染方案，Canvas 实现），切图 = 推进壁纸指针 + 通知引擎重绘；不走
  系统静态壁纸链路，从架构上根治荣耀 MagicOS 的"幽灵图"（setBitmap 成功但 Launcher 回落兜底图）
  问题。引擎**未激活时桌面不做任何切换**，只提示去系统「动态壁纸」里选中本 App。
- **锁屏：一次静态 `setBitmap(FLAG_LOCK)`**。引擎只认"系统壁纸"这一件事、同时盖住桌面和锁屏，
  没法单独控制锁屏，所以锁屏要独立设置只能走这次静态调用。
- **总开关「接管情况」**（设置抽屉）：关掉时本 App 在**系统层面完全失效** —— 两个范围回退系统，
  手动切换 / 定时 / 小组件一律不写系统（拦截点只有 `Switcher.next` 开头一处）。

> v2.0~v3.9 曾有一条「引擎未激活时自动回退静态 `setBitmap` + 多次校验重试」的兜底链路，
> v3.10 按需求删除（那条链路正是"幽灵图"的来源），上屏方式不再是"两种自动选择"。

## UI 改造方案（未实现）

下一版的重构方案与原型图见 **[`docs/ui-redesign.md`](docs/ui-redesign.md)**：首页改成壁纸库列表、
进库才是壁纸（两列正方形网格）、库级四项搬到库行右侧、抽屉只留全局项、长按浮出图标且删除统一成
红垃圾桶、接管情况一个开关 + 桌面/锁屏两行状态。

- **v2.1**：界面重设计为 Material 3 简洁浅色风（卡片分区、圆角按钮、浅灰页面底），
  小组件同步改为白色卡片 + 品牌蓝图标；全面屏/刘海屏边到边适配（内容避开状态栏、
  刘海挖孔与底部手势条，修复 Android 15 强制 edge-to-edge 导致的顶部遮挡）；功能逻辑零改动。
  主界面排版改为「顶部头部 + 内容滚动区 + 底部固定操作坞」三段式：库/定时/引擎/壁纸列表
  四张卡片可滚动，「切换桌面/切换锁屏 + 添加壁纸」常驻底部拇指可达；全部控件 id/类型不变。

- **v3.0**：主界面结构重组为「顶栏 + 底部双 Tab（壁纸 / 设置）」。壁纸列表独立成页并拿到整屏高度，
  根治 v2.1「320dp 固定高列表套在外层 ScrollView 里」的嵌套同向滚动；库选择由系统 Spinner 换成
  Material 3 暴露式下拉，范围由两个 CheckBox 换成带标签的开关行，「切换间隔」标签与数值分离、整行可点；
  空库时给出空状态引导；「切换桌面 / 切换锁屏」常驻壁纸页底部，添加壁纸改为右下角悬浮按钮；
  新建库常驻顶栏图标、删除库收进溢出菜单；标题仍显示「应用名 v版本号」。功能逻辑零改动。
- **v3.1**：壁纸列表改 2 列网格（1:1 圆角缩略图 + 标题 + 删除，整卡可点开预览；缩略图加 4MB
  内存缓存，避免网格滚动时反复解码掉帧）；全部弹窗改用 Material 3 圆角弹窗 + 描边输入框；
  裁剪页加手势操作提示与独立底部操作条；新增深色模式（跟随系统，含 values-night 配色与系统栏图标明暗）。
- **v3.2**：取消底部设置 Tab，改为从左侧滑出的设置抽屉（宽 300dp、右侧圆角，非全屏；点顶栏
  左上角图标或从屏幕左缘右滑打开），收起状态即壁纸库。顶栏标题改为「当前壁纸库名字」，版本号
  移到抽屉顶部版本行。壁纸列表由 2 列网格改为单列长条：缩略图缩成 44dp 小方块、行内以标题文字
  为主，去掉每项里的小删除按钮，改为**左滑露出右侧删除按钮**（左滑滑过一半吸附展开，同一时间
  只允许一行展开，列表滚动会自动收起）。预览弹窗的标题直接就是壁纸标题，点标题即可改名（不再
  需要「改标题」按钮）。「切换桌面 / 切换锁屏」两个按钮合并为一个：当前库范围只有桌面或只有
  锁屏时点击直接切换，两个范围都勾时先弹范围选择。
- **v3.3**：`CropView` 初始化做防御性加固（预防性改动，不是修已发生的 bug）。`resetLayout()` 是唯一
  设置 `minScale`/`maxScale`/`matrix` 的地方，而它在「位图或视图尺寸未就绪」时直接 return —— 这个
  提前返回在启动时必然发生一次（`setBitmap` 在 `onCreate` 里调用，此时视图尺寸还是 0），正常靠随后的
  `onSizeChanged()` 补救。加固做的事是：用 `matrixReady` 记录就绪状态，并在 `onDraw`、`onTouchEvent`、
  `export()` 三个入口做幂等兜底初始化，万一那次补救没发生也不会退化成「捏合被钳死在 1 倍 + 拖动挪不动
  + 导出等于原图」这种不崩溃不报错、极难定位的状态。正常路径下行为不变（初始化成功后兜底调用全是空操作）。
- **v3.4**：修复「裁剪页预览效果 ≠ 实际上屏效果」。裁剪取景框高度原本是「屏幕高 − 顶部提示 − 底部按钮条」
  （约 1080×1900，比例 0.568），而壁纸上屏区域是整屏（1080×2400，比例 0.45）；引擎绘制时会对存储图做一次
  **居中裁剪铺满**（`WallSwitchService`），于是上屏后左右各被再裁掉约 142px、整体被放大 1.263 倍 ——
  裁剪页看到的完整宽度与实际不符。改为**裁剪区铺满整屏**、提示与按钮改为浮层（不再占用布局高度），
  系统栏避让只施加在浮层容器上。这样导出图比例 = 屏幕比例 = 壁纸上屏区域，那次居中裁剪变成空操作
  （scale = 1），所见即所得。
- **v3.5**：裁剪页右上角新增两个「桌面图标预览」开关，用来判断构图主体会不会被桌面图标挡住。
  左（网格）= 内置示意网格：状态栏与 Dock 的纵向位置用真实系统栏 inset 计算，列数按实测的 4 列
  （格宽 = 屏宽/4，图标在格内居中），行数由可用高度推算 —— 无需准备，但只是近似。
  右（图片）= 选一张自己的首页截图叠加：截图与取景框同为整屏同比例，按 centerCrop 铺满后像素对齐，
  完全准确。两者都不可点击、不拦截触摸，开着也能正常捏合与拖动。只加在裁剪页：预览弹窗里的图是按
  自身比例 fitCenter 显示的，叠图标格几何不对。
  实测结论（依据一张系统裁剪器的首页预览截图 1264×2800 做像素分析）：壁纸确实铺满整屏、四周无留白
  （验证 v3.4「取景框 = 整屏」方向正确）；图标格是 4 列而非 5 列；图标只占中间偏下一条带
  （行中心约 0.476 / 0.595 / 0.714 屏高，行距约 0.119），顶部是时钟天气小组件；系统预览未绘制 Dock，
  因此 Dock 与真实排布仍需用截图模式在你机器上核对。

- **v3.6**：首页截图叠加层改为**先抠图再叠加**。v3.5 直接把整张截图半透明盖上去，截图自带的壁纸也
  跟着叠了一层，两层壁纸混在一起发灰。现在只保留图标与文字：判据是**局部细节强度**（图标与文字有
  锐利边缘和内部细节，壁纸是平滑渐变）—— ① 3×3 均值模糊去掉 JPEG 压缩噪点（否则 8×8 块噪声会被
  误判成边缘）；② 取与 8 邻域的最大亮度差，线性映射为不透明度；③ 5×5 最大值膨胀填满图标内部的
  平坦区域，否则纯色图标会只剩一圈描边。工作分辨率长边 720，纯像素运算放在后台线程，解码后约几百
  毫秒。算法先用一张真实首页截图离线跑通并肉眼确认抠图效果后才写进代码。
  同时把首页底图提升为**全局设置**：在设置抽屉新增「桌面图标预览」卡片，选一次即可（点按选/换、
  长按清除），抠好的图层存到应用私有目录；之后每次进裁剪页直接读取，不再需要每设一张壁纸就重选一次。
  在裁剪页里临时选图也会顺手存成全局底图。- **v3.7**：新增「导出目录」（SAF，ACTION_OPEN_DOCUMENT_TREE）。设置抽屉里选一个文件夹
  （如 Documents/wallswitch），之后每导入一张壁纸会自动把**全图按原始字节**复制过去
  （不解码、不重压，零画质损失）；另有「立即导出全部壁纸」把库里已有的图一次性导出。
  文件卸载后仍保留、可用文件管理器直接查看，且**不会出现在系统相册**里（这是相对 MediaStore
  方案的选择理由）。注意：卸载重装后持久化授权会失效，需要重新点一次选目录，但已导出的文件还在；
  插件不会自动把已导出的文件搬回库里，需要重新添加。- **v3.8**：修复「解码分辨率被 2 的幂采样砍掉一大半」这个上屏发虚的根因。实测一张 6736×8981 的原图
  入库后只有 700×1550（屏幕 1264×2800，仅 55%），上屏等于放大 1.8 倍。原因链：`calcSampleSize`
  用的是「一旦低于上限就停」的 2 的幂，8981 的源图 sample=4 → 解成 1684×2245，60MP 的信息在解码
  那一步就扔掉 94%；用户再在裁剪页放大一点，可用真实像素更少。改为「只要再放粗一倍仍不低于目标
  分辨率就继续放粗」，保证解码结果不小于目标；裁剪源图改走新增的 `decodeCropSource()` —— 在像素
  预算内取**最小**的 2 的幂采样，且解码后**不再二次缩小**（原来的 `decodeBounded` 采样后还会
  `scaleToFit` 缩到一个固定上限，等于把预算里还剩的清晰度又扔掉一次）。采样受堆预算约束
  （maxMemory/16 像素、封顶 24MP）防 OOM，Manifest 申请 `largeHeap`，并让 `scaleToFit` 缩放后
  立即回收原图，避免瞬时持有两份大位图。导出上限仍是屏幕长边。
  另外「桌面图标预览」去掉了内置示意网格，只保留精确的截图叠加：网格的列数/行距只能靠猜
  （写死的 4 列是从一张首页截图量出来的），换设备或换桌面布局就不准；而截图叠加按整屏同比例
  对齐，像素级准确，裁剪页右上角因此只剩一个开关。- **v3.9**：四件事一起。
  ① 裁剪页源图解码挪到后台线程：页面立刻显示、中间显示「加载中…」，解码期间「确认」置灰
     （否则点了会因为没图而报「保存失败」），「取消」照常可用。
  ② 导出改成**区域解码**（`BitmapRegionDecoder`）：拿着当前取景框的矩形回原图文件取那一块，采样取
     「该区域最长边 ≥ 屏幕长边」的最小 2 的幂 —— 于是**放大多少倍都输出满屏幕分辨率**，不再依赖
     事先猜测的放大余量；取不到区域（格式不支持等）时回退到原来的「从内存位图裁」。
  ③ 裁剪页去掉右上角按钮，改成**单击取景区**切换桌面图标预览；`CropView` 里区分轻点与拖动/缩放
     （位移 < touchSlop 且 < 300ms 才算轻点）。
  ④ 上屏链路收敛：**桌面只走动态壁纸引擎**，删掉静态 `setBitmap` 的三道/四重校验与延迟重试
     （那套是给桌面「幽灵图」兜底的，引擎架构上不存在该问题）；引擎未激活时桌面**不做任何切换**，
     只提示去打开引擎开关。**锁屏保留一次 `setBitmap(FLAG_LOCK)`** —— 引擎只认「系统壁纸」这一件事，
     同时盖住桌面和锁屏、无法单独控制锁屏，所以锁屏要独立设置只能走这次静态调用；桌面与锁屏因此
     仍可各自独立设置。新增全局「启用动态壁纸引擎」开关（引擎卡片里，替换原来的按钮）：真值取
     **系统实际状态**（`WallSwitchService.isActive`）而不存偏好值 —— 否则你在系统选择器里点了取消，
     开关还显示「开」而点切换毫无反应；打开时先把当前桌面壁纸存到 `files/previous_wallpaper.png`
     再拉起系统选择器（普通 App 没有 `SET_WALLPAPER_COMPONENT` 权限，必须用户确认一次），
     关掉时把存档设回去（`setBitmap` 会同时解除动态壁纸），没有存档则 `clear()` 退回系统默认。
- **v3.10**：把 v3.9 的「引擎开关」上提成**整个 App 的接管开关**，并让「范围」变成单选。
  ① 设置抽屉的引擎卡片改成「开启接管」总开关（`TakeoverManager`）：开关只表达**意图**，卡片下方两行
     显示**系统真值**——「桌面：当前 App / 系统」「锁屏：当前 App / 系统」（桌面看 `WallSwitchService.isActive`，
     锁屏看系统锁屏壁纸 id 是不是我们设进去那个）。打开时先把接管前的桌面/锁屏壁纸**各存一份**
     （`files/previous_home.png` / `files/previous_lock.png`），关掉时**各自还原各自**（锁屏没有存档则 `clear()`）。
     桌面接管必须由用户在系统「动态壁纸」里确认一次，用户取消则开关**自动回关**并把已做的改动还原。
     没有启用库的范围自动回退给系统，所以「打开接管但一个库都没启用」＝两边都还是系统壁纸。
  ② 新增**切换日志**（`files/switch_log.txt`，设了导出目录会同名同步一份）：只记**定时自动切换**成功的那次
     （手动切换与小组件点按不记）。每条写「切换时间：yyyy-MM-dd HH:mm」，第二条起在同一行补
     「  间隔上次切换时间：N分钟（当前定时切换设置为M分钟顺序切换）」——间隔是**实际**经过分钟（用来看漂移），
     括号里是**配置**值；`versionName` 变化时开新的一段（`v3.10` 这样的段头），段与段之间空一行。
  ③ 「范围」由「桌面 / 锁屏」两个开关改成**单选下拉**（暴露式菜单，一个库只负责一个范围），改范围要**弹窗确认**
     后才保存（保存即生效，会互斥掉另一范围内已启用的同类库），取消则下拉回到原值。手动切换按钮因此
     天然只切一个范围；老数据里既是桌面又是锁屏的库仍保留「先选范围」的兜底弹窗。
  ④ 不再让用户在「动态引擎 / bitmap」之间做选择：桌面**只有**引擎一条路（未激活时点切换只提示，不写静态壁纸），
     锁屏固定一次 `setBitmap(FLAG_LOCK)`（引擎同时盖住两个屏幕、没法单独控制锁屏）——这是实现细节，不再是选项。
  ⑤ 「开启接管」关着时，本 App 在**系统层面完全失效**：桌面与锁屏全部回退系统（打开 App / 拉开抽屉时会跑一次
     `TakeoverManager.apply` 把残留的接管还原），手动切换按钮点了只弹提示并自动推开抽屉，定时任务直接跳过
     （不写系统、不发通知、不记日志，但仍然推进记账所以状态行会如实显示「接管已关闭」），小组件点按同样只提示。
     拦截点只有一处 —— `Switcher.next` 开头（桌面引擎与锁屏 `setBitmap` 都从它走，因此手动/定时/小组件一次全覆盖）。
     库内操作（选图、裁剪、导入、导出、重命名）不受影响，否则接管关着时连壁纸库都准备不了。另外顺手修了
     手动切换壁纸后「锁屏：当前 App / 系统」这行不刷新的问题。
- **v3.11**：CI 增加**滚动的 `latest` Release**，手机上点链接直接下到 apk，不用再"下 artifact zip → 手动解压"。
  `actions/upload-artifact` 产出的 artifact 在服务端一定是 zip（`compression-level` 只是压不压缩，仍套 zip 壳），
  没有任何选项能变成裸文件；而 Release 的 asset 是原样存储的，所以走 Release 才是裸 apk。
  做法：构建完 `gh release upload latest app/build/outputs/apk/debug/app-debug.apk --clobber` —— asset 名**固定**为
  `app-debug.apk` 才能被 `--clobber` 覆盖，直链
  `https://github.com/sdnasdas/wallswitch/releases/download/latest/app-debug.apk` 因此始终指向最新构建；
  版本号/commit/构建号写进 Release 的标题与说明（放进文件名会让每次生成新 asset、`--clobber` 覆盖不到、直链失效）。
  Release 只此一个（不累积），需要 workflow 声明 `permissions: contents: write`；`gh` 在 runner 上预装，
  没有引入新的第三方 action。
- **v3.12**：设置抽屉「自动切换」卡片里加一行**「切换日志」**，点开直接读私有目录的 `files/switch_log.txt`
  弹窗展示 —— 不用再"先设导出目录、再去文件管理器翻"。弹窗正文等宽字体、可滚动、可长按选中，
  非空时给一个「复制」直接进剪贴板（粘到聊天里最省事）；没有记录时不是弹空白，而是说明什么情况才会记
  （只有定时自动切换成功才记、手动与小组件不记、接管关闭时定时跳过）。那一行本身也会回显
  **最近记录的时间**（`SwitchLog.latestTimeLabel` 从最后一条里截出 `yyyy-MM-dd HH:mm`，前缀取自
  `log_switch_time` 字符串，不写死两处），抽屉每次打开异步刷新。文件仍照旧同步一份到导出目录。
- **v3.13**：**无功能改动**（纯文档）。把下一版 UI 重构的方案与原型图放进仓库，并且写成**可直接照着实现的
  交接文档**：[`docs/ui-redesign.md`](docs/ui-redesign.md) 里有现状代码地图（逐个文件/关键方法/id）、
  逐页原型说明、已定决策、逐文件改动点、**验收清单**、三次提交计划，以及一份「这个仓库踩过的坑」
  （本地 `check.cmd` 查不出资源漏定义、`xmlns:app` 必须写 `res-auto`、`MaterialAlertDialogBuilder`
  的链式静态类型、沙箱挡 msys 命令等）。改造要点：首页改成**壁纸库列表**、进库才是壁纸的**两列正方形
  网格**、库级四项搬到**库行右侧**（启用开关 + 齿轮）、抽屉只留全局项、长按浮出图标且删除统一成
  **红垃圾桶图标**、接管情况一个开关 + 桌面/锁屏两行状态（值只写 `WallPaper`/`系统`）。
  `docs/mockups/` 是 5 张原型图，由同目录 `render.py` 用 Pillow 按真实 dp 比例渲染（色值字号取自
  `colors.xml`，可重跑再生成）。已定：**不做全局「定时切换」总开关** —— 库"启用"+"设了间隔"就到点自动切，
  要停就停用该库；抽屉只留「到点通知」开关和上次/下次两个时间。**代码尚未实现**（见文档第八节）。
  顺带修正了本文档开头"上屏方式"一段 —— 它还是 v2.0 的"两种，自动选择"写法，而 v3.10 已删掉静态兜底链路。
- **v3.14**：按 `docs/ui-redesign.md` 落地 UI 重构（原型图 `docs/mockups/`）。**首页改成壁纸库列表**
  （缩略图 + 库名 + `N 张壁纸` + 启用开关 + 齿轮；点库名就地改名；点行进库；长按浮出红垃圾桶，
  删库二次确认、确认按钮也是图标、连带清壁纸文件与切换进度）；**进库是两列正方形网格**（点格预览、
  长按浮出铅笔进裁剪页/红垃圾桶直接删、点空白或返回手势收起）；**返回键/返回手势回库列表**，
  库列表页返回才退出（`OnBackPressedDispatcher`）。顶栏标题固定 `WallPaper`，＋ 在右下角
  （库页新建库、壁纸页添加壁纸）。**抽屉只留全局项**：接管情况（一个总开关 + 桌面/锁屏两行，取值只写
  `WallPaper`/`系统`，锁屏补「引擎顺带接管」第三态，不再误报系统）、自动切换（到点通知 + 上次/下次，
  **全局定时总开关移除**：库启用+设了间隔即到点自动切）、导出与日志、桌面图标预览、引擎与后台；
  库级四项（范围/模式/间隔）+ 手动切换收进库行齿轮弹窗。左滑删除的 `SwipeRevealLayout` 随之删除，
  `EditActivity` 新增「重编已入库壁纸」模式（`EXTRA_ITEM_ID`，确认后覆盖原图）。
  **升级注意（v3.13 → v3.14，老用户）**：库的「启用 / 范围 / 间隔 / 模式」从 v3.13 起就已是**库级字段**，
  存在 `libraries.json` 的 `enabled`/`home`/`lock`/`interval_seconds`/`mode` 里；本次只废除**全局**「定时切换」
  总开关，**不做任何数据迁移**，老库照旧沿用自己原来的值（v3.13 的默认库是 `enabled=true` + 30 分钟间隔），
  因此升级后打开 App 就会开始**到点自动切换**，不需要再开任何开关（`timer_enabled` 已不读取，旧值留在
  SharedPreferences 里不再生效）；想停就到库行关掉那个库的「启用」开关 —— 关掉即退出接管，该范围还原系统。
- **v3.15**：装机反馈的四点修复。①**壁纸图不再发虚**：老版本缩略图固定「长边 256 等比」，而两列方格单格
  在 1080p 屏上约 540px，被放大 2 倍多就是马赛克 —— 缩略图改成**中心正方形 + 边长跟屏幕走**
  （`WallpaperStore.thumbSide`，上下限 384~768），并且启动时**后台一次性重做老缩略图**（只挑缺失/非正方形/
  偏小的，幂等，跑完写标记）；**库行缩略图与网格图用的是同一份文件，所以两处一起变清晰**；缩略图文件
  缺失/损坏时改为**从全图当场生成**（不再退回占位图标 —— 占位图标只留给「库里确实没有壁纸」）；
  适配器缩略图缓存 4MB → 8MB。②**预览弹窗标题就地改名**：点标题或铅笔 →
  TextView 换成 EditText（与库列表一致），回车或失焦提交，不再是「再弹一个改标题对话框」。
  ③**启用库与同范围库冲突时先提示**：新增 `LibraryStore.enabledConflicts`，启用前若撞上同范围的启用库，
  弹「继续启用会停用某某」确认；取消则把开关扳回去（原来是不问一声直接把对方关掉）。
  ④**切换日志改成两行一条**（时间 + 范围与壁纸标题；第二行缩进写实际间隔与配置），条目之间空一行；
  抽屉那行「最近记录」改为直接读记账时间戳，不再去正文截字符串；日志行**长按可清空**（老格式记录不会
  自动改写，清掉重记才整齐）。⑤**首页整页右滑可打开抽屉**：原来只靠 `DrawerLayout` 自带的边缘手势，
  而屏幕左边缘那条已被系统「返回」手势占用（系统优先），手指稍偏中间就拉不出抽屉 —— 现在列表页任意位置
  向右滑都能打开（只观察手势、从不拦截，竖向滚动不受影响；壁纸页不抢「右滑=回上一级」的直觉）。
  `versionCode 22 / versionName 3.15`。
- **v3.16**：修掉库行缩略图被"染色"的 bug —— `item_library.xml` 的 `img_lib_thumb` 带 `android:tint`
  （本是给占位图标用的），而绑定真图时**没有清 tint**，`ImageView` 的 tint 会作用到后设上去的位图
  （SRC_IN 模式），整张壁纸被染成单一灰色剪影，看起来就像"这个库没放图/没做"。现在真图路径显式
  `setImageTintList(null)`，占位路径显式设回 tint（两条路径都设，列表回收复用才不会串状态）。
  `versionCode 23 / versionName 3.16`。
- **v3.17**：**长按壁纸多一个「设为首页」图标**（仅图标、48dp、卡片底色圆形，深浅色跟随主题）——
  在库内两列网格长按任意壁纸，浮出栏从「铅笔 / 红垃圾桶」变成「铅笔 / **房子** / 红垃圾桶」：
  点房子 = 把这张立刻设为**桌面**壁纸，并把该库的桌面指针移到这里（之后自动切换就从它往下走；
  顺序模式对齐进度、随机模式清池重抽，避免紧接着又抽回同一张）。实现上把 `Switcher.next` 里
  「指针落定 + 上屏」的收口抽成 `Switcher.applyById`，新增 `Switcher.setCurrent(ctx, libId, wallpaperId,
  forHome)` 与它共用 —— 接管开关、库启用、覆盖该范围的校验与报错口径和普通切换完全一致。
  库没启用**桌面**范围时只弹提示（`set_as_home_unavailable`），**不静默改范围** —— 改范围会连带停用
  同范围的库，这种事该由用户在库行齿轮里决定；接管关着 / 引擎没激活的提示复用现有文案。
  `versionCode 24 / versionName 3.17`。